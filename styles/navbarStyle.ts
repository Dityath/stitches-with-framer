import { styled } from "../stitches.config";

export const NavbarDiv = styled("header", {
  display: "flex",
  justifyItems: "center",
  alignItems: "center",
  width: "100%",
  padding: "1rem 0",
  backgroundColor: "#333",
});
